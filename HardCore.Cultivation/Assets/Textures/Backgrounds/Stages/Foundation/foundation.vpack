# Bundled progression package, activated when the stage is first entered.
name: FoundationBackgrounds
version: 1.0.0
load: on-demand
dependencies: []
compression: balanced
platforms:
  android:
    compression: fast
